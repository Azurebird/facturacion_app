import billingCreate
  from '../../../../../server/api/billing/service/actions/billing.service.create';

describe('Tests for the billing creation', () => {
  it('[ERROR] When the required parameters are not sent', async () => {
  });
});
